import QtQuick 2.12

ModernButton {
    // Simple wrapper with fixed size
    width: 140
    height: 45
    primaryColor: '#3ce759'
    hoverColor: "#c0392b"
    pressedColor: "#a93226"
    textColor: "#ffffff"
    disabledColor: "#bdc3c7"
    borderRadius: 8
}
